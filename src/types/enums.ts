export enum EMAIL_DOMAIN {
  GOOGLE = "gmail.com",
  NAVER = "naver.com",
}

export enum INFO_TYPE {
  USER_NAME,
  USER_PW,
  NAME,
  USER_SSN,
  USER_EMAIL,
  CODE_VERIFICATION,
}

export enum VERIFICATION {
  EMAIL = "email",
  CODE = "code",
}
