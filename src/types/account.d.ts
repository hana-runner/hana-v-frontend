interface AccountInfoType {
  bankName: string;
  accountNumber: string;
}

interface AccountType {
  id?: number;
  accountNumber: string;
  balance: number;
  accountName: string;
  accountType: string;
}
